// Native map component — only loaded on iOS/Android
import { default as RNMapView, Marker as RNMarker } from 'react-native-maps';

export const MapView: any = RNMapView;
export const Marker: any = RNMarker;
