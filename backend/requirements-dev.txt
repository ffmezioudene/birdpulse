# Dev/test-only tooling. NOT installed in the production Docker image.
# Install locally with:  pip install -r requirements-dev.txt
#
# Keeps the production image small (~150 MB lighter without these).

-r requirements.txt

pytest>=8.0.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
